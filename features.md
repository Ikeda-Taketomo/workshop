# ポモドーロタイマー Web アプリケーション 機能一覧

## 目的

このドキュメントは、ポモドーロタイマー Web アプリケーションを作成するにあたって実装が必要な機能を整理したものです。

機能は、初期リリースで必要な MVP 機能と、後から追加できる拡張機能に分けて管理します。

## MVP 必須機能

### 1. タイマーモード切り替え

- 集中モードを選択できる
- 短い休憩モードを選択できる
- 長い休憩モードを選択できる
- 選択中のモードが画面上で分かる
- モード変更時に残り時間をそのモードの初期時間へリセットする

### 2. タイマー表示

- 残り時間を `25:00` のような分秒形式で表示する
- 現在のモード名を表示する
- 実行中、一時停止中、完了時の状態を画面に反映する
- 残り時間が 0 未満にならないように表示する

### 3. タイマー操作

- タイマーを開始できる
- タイマーを一時停止できる
- 一時停止したタイマーを再開できる
- タイマーをリセットできる
- 実行中の操作状態に応じてボタン表示やボタン状態を切り替える

### 4. 時間計算

- 開始時刻と現在時刻の差分から残り時間を算出する
- `setInterval` の実行回数だけに依存しない
- ブラウザタブが非アクティブになっても時間のズレを抑える
- 残り時間が 0 になったことを検知する

### 5. セッション完了処理

- 残り時間が 0 になったらセッション完了として扱う
- 集中セッション完了時に完了数を増やす
- 集中セッション完了後に次の休憩モードへ遷移する
- 通常時は短い休憩へ遷移する
- 規定回数の集中セッション完了後は長い休憩へ遷移する
- 休憩完了後は集中モードへ遷移する

### 6. 設定機能

- 集中時間を変更できる
- 短い休憩時間を変更できる
- 長い休憩時間を変更できる
- 長い休憩に入るまでの集中回数を変更できる
- 不正な設定値を保存できないようにする
- 未開始またはリセット済みのタイマーに設定変更を反映する

### 7. 設定・状態の保存

- `localStorage` に設定を保存する
- ページ再読み込み後も設定を復元する
- 完了した集中セッション数を保存する
- 最後に選択したモードを保存するか検討する
- 保存データが壊れている場合は初期値へ戻す

### 8. UI 更新

- 残り時間をリアルタイムに更新する
- 選択中モードの見た目を切り替える
- タイマー実行状態に応じて操作ボタンを更新する
- 設定変更時に表示へ反映する
- 実行中は必要に応じてモード変更や設定変更を制御する

### 9. Go サーバー機能

- `/` でアプリケーションの HTML を返す
- `/static/...` で CSS、JavaScript、画像などの静的ファイルを配信する
- `/healthz` でヘルスチェックを返す
- サーバー起動時に待ち受けポートを指定できるようにする

### 10. 基本的なテスト

- タイマー計算のユニットテストを追加する
- 状態遷移のユニットテストを追加する
- 設定値バリデーションのユニットテストを追加する
- `localStorage` ラッパーのユニットテストを追加する
- Go の HTTP ハンドラテストを追加する

## 拡張機能

### 1. 通知機能

- セッション完了時に通知音を鳴らす
- ブラウザ通知を表示する
- 通知のオン・オフを設定できる
- 通知音のオン・オフを設定できる

### 2. 進捗表示

- 今日の完了ポモドーロ数を表示する
- 現在のサイクル数を表示する
- 次のモードが短い休憩か長い休憩かを表示する
- 日別の簡単な達成状況を表示する

### 3. セッション履歴

- 完了した集中セッションを記録する
- セッションの開始時刻を保存する
- セッションの終了時刻を保存する
- セッションのモードと時間を保存する
- セッション履歴一覧を表示する

### 4. タスク管理

- 作業対象のタスク名を入力できる
- セッションとタスクを紐付ける
- タスクを完了済みにできる
- タスク別の完了セッション数を表示する

### 5. アクセシビリティ

- キーボードだけで主要操作を実行できる
- ボタンに適切なラベルを付ける
- 色だけに依存しない状態表示を行う
- スクリーンリーダー向けに残り時間の更新を扱う
- フォーカス位置が分かる見た目を用意する

### 6. レスポンシブ対応

- PC 画面で使いやすいレイアウトにする
- モバイル画面で使いやすいレイアウトにする
- 小さい画面でもタイマー表示と操作ボタンが重ならないようにする
- 設定パネルが狭い画面でも操作しやすいようにする

### 7. E2E テスト

- 画面を開けることを検証する
- タイマーを開始できることを検証する
- タイマーを一時停止できることを検証する
- タイマーをリセットできることを検証する
- 設定変更が画面と状態に反映されることを検証する

## 実装優先順位

最初は、タイマーが正確に動き、モード遷移し、設定を保存できるところまでを MVP とします。

1. Go で HTML、CSS、JavaScript を配信する
2. タイマー表示 UI を作る
3. `timer.js` に時間計算と状態遷移を実装する
4. 開始、一時停止、再開、リセットを実装する
5. 集中、短い休憩、長い休憩の切り替えを実装する
6. セッション完了時の自動遷移を実装する
7. 設定変更と `localStorage` 保存を実装する
8. ユニットテストを追加する
9. 通知、履歴、タスク管理などの拡張機能を追加する

## MVP 完了条件

MVP は、次の条件を満たした時点で完了とします。

- ブラウザでポモドーロタイマー画面を表示できる
- 集中、短い休憩、長い休憩を切り替えられる
- タイマーを開始、一時停止、再開、リセットできる
- セッション完了時に次のモードへ遷移できる
- 長い休憩までのサイクルを管理できる
- タイマー設定を変更して保存できる
- ページ再読み込み後も設定が復元される
- タイマー計算と状態遷移のユニットテストが通る
- Go の HTTP ハンドラテストが通る